# M0 验证记录（2026-08-19）

## 自动验证结果

- Xcode Debug 构建：通过；
- Swift Testing：11 项通过；
- XCTest UI：基础启动、列表行跨窗口动画、浅色启动、深色启动共 4 次执行通过；
- 列表行起点通过 `NSView` 转换为屏幕坐标，并成功直线移动到右下角测试鱼缸点；
- backing scale 像素对齐、非零屏幕原点、直线插值和端点误差边界均有单元测试；
- `TransitionOverlayPanel` 不成为 key/main window、忽略鼠标，并在屏幕参数变化或休眠时取消动画；
- `git diff --check`：通过。

## 当前自动化覆盖

- 单显示器与刘海显示策略；
- 无刘海、外接多屏、镜像和全屏隐藏策略分支；
- 最多 8 个命中目标的最近中心选择；
- 1、3、8 个 Metal metaball 参数入口；
- SwiftUI 列表行到鱼缸点的跨窗口动画；
- 1x/2x backing scale 的屏幕坐标转换与端点误差。

## 仍需人工实机验证

- 刘海测试点到鱼缸点的视觉路径；
- 动画期间在 Xcode、Finder、Safari、Keynote 与游戏/高刷新窗口间切换焦点；
- Safari 视频、Keynote、Xcode、Finder 等全屏场景；
- 动画期间连接/断开显示器，确认立即取消且不残留 Overlay；
- 100 次悬停和 100 次跨窗口动画的窗口/内存观察；
- 30 分钟 Metal 稳态 CPU、内存与 GPU 基线。

## 已知环境事项

本机 Xcode 的 Metal Toolchain 组件下载返回资产目录获取失败。M0 Spike 暂时通过
`MTLDevice.makeLibrary(source:)` 编译相同的原生 MSL shader，不改变 Metal 技术路线；
生产构建应在官方组件可用后恢复构建期 shader 编译。
